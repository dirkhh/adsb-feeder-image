# GitHub Webhook Service Secrets
# Copy this file to /etc/github-webhook/secrets.env and update with your actual secrets
# Set permissions: sudo chmod 600 /etc/github-webhook/secrets.env

# GitHub webhook secret (from GitHub repository webhook settings)
# Generate with: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
GITHUB_WEBHOOK_SECRET=REPLACE_WITH_YOUR_WEBHOOK_SECRET

# Boot test API configuration
# Use your Tailscale IP (100.x.x.x) or localhost for secure communication
BOOT_TEST_API_URL=http://100.x.x.x:9456/api/trigger-boot-test

# Boot test API key (from generate-api-key.py)
BOOT_TEST_API_KEY=REPLACE_WITH_YOUR_API_KEY
