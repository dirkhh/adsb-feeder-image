# Remote Deployment Configuration
#
# Copy this file to .env and configure your deployment targets
#
# Format: [user@]hostname
#   - If user is omitted, defaults to 'root'
#   - Hostname can be IP address or domain name
#   - SSH key authentication must be configured

# GitHub Webhook Service Host
# Example: WEBHOOK_HOST=root@webhook.example.com
# Example: WEBHOOK_HOST=admin@192.168.1.10
# Example: WEBHOOK_HOST=webhook.example.com  (defaults to root@)
WEBHOOK_HOST=

# Boot Test Service Host
# Example: BOOT_TEST_HOST=root@boottest.example.com
# Example: BOOT_TEST_HOST=192.168.1.20  (defaults to root@)
BOOT_TEST_HOST=

# Notes:
# - Both services can be on the same host
# - Leave blank to skip deployment of that service
# - Requires SSH key-based authentication (no password prompts)
# - User must have sudo/root privileges on remote hosts
